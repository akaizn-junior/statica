name: Deploy statica site to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install statica
        run: cargo install --path crates/statica-cli --locked

      - name: Build with statica
        run: statica ./statica-landing

      - name: Validate landing artifact
        run: |
          test -f statica-landing/.dist/index.html
          test -f statica-landing/.dist/en/index.html
          test -f statica-landing/.dist/pt/index.html
          test -f statica-landing/.dist/public/styles.css
          grep -q 'href=../public/styles.css' statica-landing/.dist/en/index.html
          grep -q 'href=../public/styles.css' statica-landing/.dist/pt/index.html

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: statica-landing/.dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
