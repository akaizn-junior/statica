---
description: statica Rust engine conventions
globs: "crates/**/*.rs"
alwaysApply: false
---

# statica Rust

Read [AGENTS.md](../../AGENTS.md). Crate-specific: [statica-core/AGENTS.md](../../crates/statica-core/AGENTS.md), [statica/AGENTS.md](../../crates/statica/AGENTS.md).

## Boundary

- `statica-core`: pipeline only — never reads `statica.toml`
- `statica` CLI: config, flags, watch/serve → maps to `BuildOptions`

## Style

- Core errors: `statica_core::Error` + `Diagnostic` for authoring mistakes
- CLI errors: `anyhow` with `.context()`
- Tests: call `build(&opts)`, assert emitted HTML; see `build_fixture.rs`
- Clippy pedantic in core (see `lib.rs` allows)
- html5ever for parsing — not regex

## Before submitting

```bash
cargo test -p statica-core
cargo test -p statica
cargo build -p statica --release
```

Dogfood new features in `examples/blog` first.
